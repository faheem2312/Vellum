[package]
name = "orderbook"
version = "0.1.0"
edition = "2024"

[dependencies]
protocol = { path = "../protocol" }

[dev-dependencies]
criterion = { version = "0.5", features = ["html_reports"] }

[[bench]]
name = "matching"
harness = false